<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash screen. -->
    <style name="SplashScreenTheme" parent="Theme.Radio">
        <item name="android:windowBackground">@drawable/layout_splash_screen</item>
        <item name="android:navigationBarColor">?attr/colorPrimary</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Radio" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryVariant">@color/white_day_light</item>
        <item name="colorOnPrimary">@android:color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange_500</item>
        <item name="colorSecondaryVariant">@color/orange_dark</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <item name="android:windowBackground">?attr/colorPrimary</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Text color. -->
        <item name="android:textColor">?attr/colorOnPrimary</item>
    </style>
</resources>